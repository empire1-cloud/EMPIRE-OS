from fastapi import APIRouter
from app.engine.orchestrator import run_empire_cycle

router = APIRouter()

@router.post("/repo")
def ingest(payload: dict):
    repo_url = payload["repo_url"]

    result = run_empire_cycle(repo_url)

    return {
        "status": "processed",
        "result": result
    }
